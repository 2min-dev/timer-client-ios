# Uncomment the next line to define a global platform for your project
  platform :ios, '10.0'

target 'timerset-ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for timerset-ios

  # RxSwift
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'RxDataSources', '~> 3.0'
  pod 'ReactorKit'
  pod 'SnapKit', '~> 4.0.0'

  # Alamofire
  pod 'Alamofire'

  # Realm
  # pod 'RealmSwift'
  
  # SwiftLint
  pod 'SwiftLint'
  
  # Log
  pod 'SwiftyBeaver'
  
  target 'widget' do
    inherit! :search_paths
  end
end
